# hello-http

Progressive series on Go's `net/http`, no framework. Each `hello_NN_topic.go`
file is a complete, standalone server — run it directly with the command in
its header comment, then hit it with the curl example given there.
No file depends on another.
