// module: import path other packages would use to reference this one (irrelevant
// here since every lesson file is package main, run standalone via `go run`)
module hello-go

// go: minimum Go language/toolchain version this module requires
go 1.25.0
